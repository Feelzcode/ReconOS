NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Must match DEMO_MODE_SECRET on the backend exactly, or the mock webhook
# trigger in Demo Mode will return 403. Only set this on the Demo Day
# deployment — remove/blank it everywhere else.
NEXT_PUBLIC_DEMO_SECRET=
